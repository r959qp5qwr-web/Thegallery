# Stage 0 receipt — Doctrine assimilation of The Gallery

**Date:** 2026-09-04 · **Session:** `a00ba519-2d7c-5264-8383-31d652d2ffcf` (Claude Code; remote session `cse_01SgyQEk49Xm5Xh5beQZ8JpC`) · **Result:** orientation VALID; Stage 1 authorised.

This document is committed evidence of what the binding session actually inspected, created and validated. The live orientation receipt (`.doctrine/runtime/`) is regenerated by every session and is never committed; the governing files remain `doctrine/DOCTRINE_BINDING.json` and `doctrine/PRODUCT_STATE.json`.

## 1. Ground truth recorded before any mutation

| Repository | Role | Remote | Branch | Exact HEAD | Worktree |
|---|---|---|---|---|---|
| `r959qp5qwr-web/builders-doctrine` | Read-only central Doctrine authority | `https://github.com/r959qp5qwr-web/builders-doctrine` | local `claude/the-gallery-foundation-7plo1e`, identical to `origin/main` and local `main` (0 ahead / 0 behind) | `dfdecb651df8cef5387487fee21d8ee2f798db95` — "Record Governor decisions 2026-08-16; Doctrine advanced to v1.1.0" (2026-08-16) | clean |
| `r959qp5qwr-web/Thegallery` | Writable product repository and base | `https://github.com/r959qp5qwr-web/Thegallery` | unborn `claude/the-gallery-foundation-7plo1e`; the remote held no refs, so no `main` existed | none (no commits) | empty |

- Central Doctrine version at inspection: `VERSION.json` **1.1.0**, status **ACTIVE_PENDING_REPIN** (authoritative on the default branch; not frozen until three other products re-pin). Open central obligations noted, none affecting this binding: OBL-UXDA-001, C-0006.
- Handoff package `THE_GALLERY_FABLE_5_HANDOFF_v1.1.zip` sha256 `db781afa096c8c72763b5be49ebb580bf2fc75d73c2d50b290de3fa475f5c6f0`. Its Canon and Audit Protocol snapshots (dated 2026-08-08) differ from the bound live documents (187 and 243 diff lines respectively) and were used only as orientation context, never as authority.
- Existing product-local governance or source: none.

## 2. The live Doctrine mechanism used

1. Read `README.md`, `VERSION.json`, `modules/README.md`, the Canon, Playbook, Audit Protocol, Candidate Register, the assimilation report of 2026-08-16 and all 20 `ux-design-assurance` v1.1 module files in the live checkout.
2. Located the canonical entry point named by the README: `operating/doctrine_bootstrap.py` (`orient` / `check`) with `operating/DOCTRINE_BINDING.template.json` and `operating/PRODUCT_STATE.template.json`.
3. Validated the supplied manifest before executing anything: all five `VERSION.json` core fingerprints (Canon, Playbook, Audit Protocol, Candidate Register, bootstrap) match the files on disk; the module manifest fingerprint matches; all 19 module files match the manifest's SHA-256 and byte counts.
4. Implemented the README's stated caller obligation ("the caller must fingerprint this executable against the binding before running it") as `scripts/doctrine-orient.py`, which also verifies the checkout HEAD equals the bound commit, refuses a dirty working tree on bound paths, cross-checks `VERSION.json`, and expands the module manifest.
5. Bound the exact commit derived live (`git rev-parse origin/main`), not copied from any other product.

## 3. Product-local artefacts created

| Path | Purpose |
|---|---|
| `doctrine/DOCTRINE_BINDING.json` | Binding to commit `dfdecb651df8…` with seven pinned artefacts (five core, `VERSION.json`, module manifest); status ACTIVE; Governor decision GAL-GOV-2026-09-04-STAGE0 |
| `doctrine/PRODUCT_STATE.json` | Product sentence; 29 FIXED decisions with enforcement loci and states; 7 mandated outcomes; 7 recommended/candidate decisions; 22 refusals with reopen conditions; 16 OPEN governor decisions with current recommendations; promise table of 20 rows (all INTENDED); 10 obligations/unknowns; active stage and next acceptance condition |
| `doctrine/BUILD_LEARNING_LEDGER.md` | Three Stage 0 incidents captured |
| `doctrine/harvests/README.md` | H1 harvest placeholder note |
| `scripts/doctrine-orient.py` | Product-local seam: `orient`, `check`, `assess`, `status` |
| `scripts/doctrine-hook.py` | Claude Code SessionStart (automatic orientation, standing assessment) and PreToolUse guard (GUARD-1 read-only Doctrine, GUARD-2 orientation before consequential acts, GUARD-3 no direct commits on the default branch); fails open on its own malfunction |
| `scripts/doctrine-drill.py` | 28-case negative drill on scratch clones with stamped artifacts |
| `.claude/settings.json` | Hook wiring |
| `.githooks/pre-commit`, `.githooks/pre-push` | Git-level consequence gates; `core.hooksPath` set by the SessionStart hook |
| `ux/UX_MANIFEST.yaml` | Six critical journeys IDENTIFIED under `ux-design-assurance` 1.1.0 |
| `CLAUDE.md`, `README.md`, `.gitignore` | Discovery only; no volatile facts |

## 4. Validations run

### 4.1 Orientation (`python3 scripts/doctrine-orient.py orient`)

```text
DOCTRINE ORIENTATION: VALID — receipt written to .doctrine/runtime/DOCTRINE_ORIENTATION_RECEIPT.json
doctrine: dfdecb651df8 on claude/the-gallery-foundation-7plo1e · version 1.1.0 (ACTIVE_PENDING_REPIN)
module files verified: {'ux_design_assurance_manifest': 19}
receipt inputs: binding 904f8d301e833d4c… state 6316439c5016a5a5…
continuity: fixed 29 · open 16 · unresolved 10 · release UNKNOWN
```

### 4.2 Consequence gate (`python3 scripts/doctrine-orient.py check`)

```text
DOCTRINE GATE: PASS — valid orientation for session a00ba519-2d7c-5264-8383-31d652d2ffcf
```

### 4.3 Drill (`python3 scripts/doctrine-drill.py`, stamp `20260904T055244Z`)

The central Doctrine supplies no drill or negative test for the bootstrap/binding mechanism; that limit is recorded under NOT SEEN. The drill below is product-local. Counts: {'PASS': 28, 'DECORATIVE': 0, 'STALE': 0, 'RED': 0}.

| Case | Verdict | What was proven | Law |
|---|---|---|---|
| D-01 | PASS | baseline: seam orient against the real checkout is green | L-A8 — mutations run against a proven-green baseline |
| D-02 | PASS | baseline: seam check for the same session is green | Playbook §3 consequence gate |
| D-03 | PASS | Canon byte-tampered in a clone → central bootstrap refuses | README fail-closed: bound artifact hash mismatch |
| D-04 | PASS | Canon tampered in a clone → seam sees the dirty working tree before the bootstrap runs | README: a git-HEAD pin cannot see a working-tree edit |
| D-05 | PASS | Canon removed (subject removal) → central bootstrap refuses | L-A8 — every gate carries a subject-removal mutation |
| D-06 | PASS | Candidate Register tampered → central bootstrap refuses (trial defect B6 class) | VERSION.json prior_propagation B6 |
| D-07 | PASS | bootstrap tampered → seam refuses BEFORE executing it (marker never written) | README: fingerprint the bootstrap before running it |
| D-08 | PASS | checkout on the parent commit → seam refuses: bound commit mismatch | Commission §3: bind to the exact inspected commit |
| D-09 | PASS | module file tampered → manifest expansion refuses (the central bootstrap alone would not see this) | Canon: derive every guarded set |
| D-10 | PASS | binding claims another Doctrine version than VERSION.json → contradiction refused | Commission §3: contradictory authority is BLOCKED |
| D-11 | PASS | binding status not ACTIVE → refused | bootstrap: binding must be ACTIVE |
| D-12 | PASS | required continuity field missing from product state → refused | bootstrap REQUIRED_STATE_KEYS |
| D-13 | PASS | product id differs between binding and state → refused | bootstrap: binding/state identity |
| D-14 | PASS | receipt from another session → check refuses | bootstrap: every blank session must orient |
| D-15 | PASS | governing state edited after orientation → check reports STALE | Playbook §3 invalidation |
| D-16 | PASS | Doctrine root missing → seam refuses | Commission §3: unavailable authority is BLOCKED |
| D-17 | PASS | guard: `git commit` with no orientation for the session → DENIED | L-A21 pre-act denial |
| D-18 | PASS | guard: `git commit` with valid orientation → allowed (anti-vacuous) | Canon: pair every must-not with proof the legitimate case passes |
| D-19 | PASS | guard: commit inside builders-doctrine → DENIED regardless of orientation | Commission §3 read-only authority |
| D-20 | PASS | guard: `git push` to a builders-doctrine remote URL → DENIED | Commission §3 read-only authority |
| D-21 | PASS | guard: reading builders-doctrine → allowed | reads are never denied |
| D-22 | PASS | guard: benign command → allowed | guard asks only about consequential acts |
| D-23 | PASS | guard: malformed hook input → fails OPEN with a notice | Governor ruling 2026-08-09: fail-open default |
| D-24 | PASS | guard: commit on the default branch → DENIED | Canon Day Zero #6 branch discipline |
| D-25 | PASS | wiring: hooks registered, match Bash, reference existing scripts, output not discarded; git hooks present | L-A21 fail-closed wiring gate |
| D-26 | PASS | assessment census against a planted fixture reports exactly the planted counts | L-A20 anti-vacuous derivation self-test |
| D-27 | PASS | the drill's own verdict grammar distinguishes DECORATIVE from STALE | L-A18 point the honesty instrument at itself |
| D-28 | PASS | Doctrine checkout untouched | Commission §3 read-only |

### 4.4 Git-hook layer, run by hand

```text
--- git-hook layer: pre-commit with this session (expect exit 0) ---
exit=0
--- git-hook layer: pre-commit with a foreign session id (expect refusal) ---
DOCTRINE GATE: FAIL — receipt belongs to a different session; every blank session must orient
doctrine gate: orientation invalid — commit refused. Run: python3 scripts/doctrine-orient.py orient
exit=1
--- git-hook layer: pre-push to a builders-doctrine URL (expect refusal) ---
doctrine gate: push to builders-doctrine refused — the central Doctrine is read-only from this product (Commission v1.1 §3).
exit=1
--- git-hook layer: pre-push to the product remote with this session (expect exit 0) ---
exit=0
```

## 5. Proof state

**SEEN** (each with an artifact in this session)
- All seven bound fingerprints and the 19 module files match the checkout on disk (drill D-01, seam receipt).
- The checkout HEAD equals the bound commit and equals `origin/main` (git output in §1; seam receipt).
- Orientation VALID and check PASS for this session (§4.1, §4.2).
- Every drilled protection fails under its violation and passes its legitimate case (§4.3, 28 PASS, stamped artifacts under `.doctrine/runtime/drills/20260904T055244Z/`).
- The guard's decisions, exercised by invoking `scripts/doctrine-hook.py` exactly as the harness would, with JSON on stdin (D-17 to D-24).
- The git-hook gates refuse a foreign session and a builders-doctrine push and allow the legitimate case (§4.4).
- builders-doctrine unchanged: `git status` clean and HEAD identical before and after orientation, check and both drill runs (D-28).

**NOT SEEN**
- Claude Code executing the SessionStart and PreToolUse hooks at runtime. The wiring is registered and drilled (D-25), but the settings were created during this session and hook liveness cannot be proven from inside the repository (Canon, "The hook surface"). Enforcement at the hook layer is ARMED-as-configured; the git hooks and the explicit `check` command are the downstream layers. Evidence path: the standing assessment printed at the start of the next session.
- A central drill for the bootstrap: none exists in builders-doctrine; the protection is proven only by the product-local drill.
- Any CI or trunk-protection layer: none exists yet (OBL-GAL-003).
- Behaviour of the hooks under shells other than POSIX `sh` and Python 3.11.

**UNKNOWN**
- Whether OBL-UXDA-001 (re-pin of maa.se, Ustaad and Ahoy) has been discharged; outside this session's scope and not affecting this binding.
- GitHub branch protection and whether project hooks are trusted in this managed environment (dashboard-only facts).

**BLOCKED:** none.

## 6. builders-doctrine remained read-only

No commit, push, checkout, file edit or config change was made in the Doctrine checkout. The seam and drill invoke git with `GIT_OPTIONAL_LOCKS=0` and run the bootstrap with bytecode writing disabled; the drill operates on scratch clones and asserts the real checkout is byte-for-byte unchanged (D-28). The guard denies mutating commands aimed at the checkout (D-19, D-20) and `.githooks/pre-push` refuses its remote URL.

## 7. Authorised next action

Stage 1 — product and interface architecture, under `active_stage` in `doctrine/PRODUCT_STATE.json`. Application source, schema and deployment configuration remain blocked until the governor accepts the Stage 1 direction.

## 8. Observations, non-blocking

- Doctrine 1.1.0 is not frozen; any advance of its default branch stales this binding by design (OBL-GAL-004).
- builders-doctrine tracks `operating/__pycache__/doctrine_bootstrap.cpython-311.pyc`, a compiled shadow of the fingerprinted executable (OBS-GAL-001; already recorded centrally).
- The product repository had no `main`; governance is commit #1 on the designated branch and the governor's merge establishes `main` (OBL-GAL-005).
